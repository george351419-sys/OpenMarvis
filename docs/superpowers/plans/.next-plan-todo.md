# Next Plan Queue

After v1.0.0 (released 2026-06-03，M3 整体落地：App Agent / Skill / Scheduler / Timeline)
下一步候选 — **优先级和先后顺序待用户决定**。

## P0 — 真实运营需要

- **OpenTelemetry / 性能 metrics**：tool call P50/P95、LLM token 用量、Schedule 触发成功率、SSE 流断点率
- **生产部署 baseline**：systemd / launchd 服务化 + healthcheck + 日志轮转

## P1 — 跨模型扩展

- **多 model adapter 测试**：当前默认 Anthropic；LiteLLM 透传层在 OpenAI / Gemini / DeepSeek / Ollama 上的实际表现（tool_use 兼容性、SSE chunk 形状差异）
- **vision_locate 改 prompt 而不是模型**：现在 hard-code Claude，改成跟 settings.llm.provider_model 对齐

## P2 — 用户体验

- **Tauri 打包**：把 backend + frontend bundle 成单一 macOS .app
- **会话历史 search / filter**：现在会话列表只能滚
- **mobile-responsive**：Timeline 在窄屏抽屉化（M3 design §5.2 提到了但没做）

## P3 — 生态

- **Skill 商店 baseline**：`~/.openmarvis/skills/` 目前是裸目录，可以加 manifest index 和签名验证
- **第二个内置 Skill**：plan §3 列了 `ppt_generate` 留给 v1.x
- **VitePress / Astro 文档站**：当前文档全在 markdown，没有静态站

## 显式不做

- Android 模拟器集成 — 砍掉了
- Windows / Linux 移植 — 永不

---

## 历史 todo（已完成）

- v0.5.0 (2026-06-02)：Browser Agent / Computer Agent / Spotlight / Timeline 雏形 — 已发布
- v1.0.0 (2026-06-03)：App Agent / Skill / Scheduler / Timeline 完整版 — 已发布
