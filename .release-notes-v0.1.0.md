# OpenMarvis v0.1.0 — MVP

首个公开版本。在 macOS 上跑通 Marvis-like 桌面智能体的最小闭环：**Main Agent + File Agent + Search Agent**，以及完整的分层调度、卡片协议、三级安全模型。

## 亮点

- **分层调度协议**：`Sub Agent → Skill → Tool → 代码兜底`，沿用 Marvis 设计；结构化任务派发 `<overall_goal>/<current_task>/<attachments>`。
- **5 类 14 个内置工具**：read/write/edit/delete/list_dir/search_files/shell/python/web_search/web_fetch/analyze_image/ask_user/dispatch_task/present_result。
- **三层安全责任链**：PathGuard（路径 + 通配符 + 敏感名）、CmdGuard（高危命令模式）、CredentialGuard（API key 脱敏）→ SecurityGate 聚合。
- **流式 SSE 协议**：`thinking_delta / content_delta / tool_call_start / tool_call_result / card / ask_user / sub_agent_start / sub_agent_end / warning / error / done`。
- **`mv-*` 卡片渲染**：mv-file-list / mv-image-gallery / mv-video-card / mv-delete-list / mv-product / mv-tool-call / mv-ask-user，前端 react-markdown 自定义 code renderer 拦截。
- **会话独立 workspace**：`~/.openmarvis/workspaces/conv_<id>/{uploads,temp,output}/`。
- **产物声明 + 写入审计**：后端 `WriteAuditLog` 自动校验 `mv-product` 声明的文件是否真实存在/已写入。

## 技术栈

- 后端：Python 3.11 + FastAPI + Pydantic + SQLModel + LiteLLM（默认 `claude-opus-4-7`）+ sse-starlette + Playwright(Python)
- 前端：Next.js 14 + React 18 + Tailwind + shadcn/ui + Zustand + react-markdown
- 仓库：pnpm workspace monorepo
- License：Apache 2.0

## 验收

- 后端单元测试：**85 passing，整体覆盖率 90%**
- 关键模块覆盖率：path_guard 96% / policy 98% / dispatch 88% / chat 89% / agents/base 85% / llm/client 95%
- 前端 typecheck + production build 通过
- Playwright E2E 配置 + 3 场景就位（待用户提供 ANTHROPIC_API_KEY 触发实跑）

## 快速开始

前置：Python 3.11、Node 20、pnpm 9。

```bash
git clone https://github.com/george351419-sys/OpenMarvis.git && cd OpenMarvis
make install
export ANTHROPIC_API_KEY=...
make dev
```

打开 http://localhost:3000，新建会话即可。

## v0.1.0 范围说明

**已实现**：Main Agent + File Agent + Search Agent + 完整工具链 + 安全网关 + 卡片协议 + 产物校验 + Web UI。

**待后续版本**（已在路线图）：
- v1.5：Voice（STT/TTS 双工）+ 本地 RAG 知识库
- v2.0：Windows 平台移植（Computer / App Agent 等价实现）
- v3.0：多用户 + RBAC + 移动端伴侣
- v4.0：Plugin Marketplace（签名 + 沙箱 + 发布流）

**永久不做**：Linux 平台。

**M2-M3 即将启动**（继 v0.1.0 之后）：Browser / Computer / App Agent + Skill 体系 + 定时任务。详见 [`docs/superpowers/plans/.next-plan-todo.md`](docs/superpowers/plans/.next-plan-todo.md)。

## 致谢

架构灵感来自腾讯 Marvis（Windows 桌面 AI 助手）。本项目是开源致敬实现，移植到 macOS 并将协议、安全策略、卡片渲染等设计公开化、社区化。

---

文档：
- 架构 spec：[`docs/superpowers/specs/2026-06-01-openmarvis-design.md`](docs/superpowers/specs/2026-06-01-openmarvis-design.md)
- 实施计划：[`docs/superpowers/plans/2026-06-01-openmarvis-m0-m1-mvp.md`](docs/superpowers/plans/2026-06-01-openmarvis-m0-m1-mvp.md)
- 贡献指南：[`CONTRIBUTING.md`](CONTRIBUTING.md)
